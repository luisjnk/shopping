export type IProduct = {
  id: string,
  createdAt: string,
  productName: string,
  price: number,
  product: string,
  departament: string,
  color: string,
  material: string,
  adjectives: string
}
